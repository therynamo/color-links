'use strict'; // Using Node 4.2.3 ES2015 features (non-transpiled)
const expect = require('chai').expect;

describe('content-script', () => {
  describe('#setStyleSheet', () => {
    it('should return ', () => {
      expect(1).to.equal(1);
    });
  });
});
